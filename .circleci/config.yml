version: 2.1
orbs:
  node: circleci/node@5.1.0

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      # Whatever you want to do
      - node/install:
          install-yarn: true
          node-version: '16.13'
      - run: npm install
      - run: npm start
      